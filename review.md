# 学习工作台 · 代码评审报告

> 评审对象：`study_workspace.html`（单文件 Web App，约 3500 行 / 165KB JS）
> 评审方式：静态阅读 + 在 stub DOM 中实跑核心逻辑（数据层、积分系统、C++ 模拟器）
> 结论：**骨架扎实、UI 完整、可直接用；但「C++ 编程备考」模块的在线运行器存在致命 Bug，所有带循环/条件判断的代码都会跑飞。**

---

## 一、项目概览（我看到了什么）

这是一个**初二全科学习工作台**，单 HTML 文件、纯前端、数据存 localStorage，已打包成鸿蒙/PWA/Android WebView 三种安装方式。

功能模块（侧边栏 6 个 Tab）：

1. **今日学习台** — 任务清单 + 番茄钟 + 课程表，逾期任务自动滚动到当天
2. **全科错题本** — 按科目筛选、错题状态（复习中/已掌握）、间隔复习提醒
3. **题库训练** — 9 科选择题/填空/判断，答题反馈 + 解析
4. **C++ 编程备考** — 课程+例题+**内置 C++ 代码运行器（playground）**，这是 CSP-J 备考核心
5. **学习统计** — 热力图、科目分布、柱状图（纯手写 SVG/canvas 风格）
6. **成长中心** — 积分、等级、成就、每日挑战、奖励兑换（游戏化激励）

工程层面：代码组织清晰、组件化 CSS、暗/亮主题、移动端响应式、有导出/导入备份。**整体完成度很高。**

---

## 二、严重问题（必须修）

### 🔴 P0 — C++ 模拟器无法判断大小比较，循环/条件全部失效

**根因**：`simulateCpp` 里的 `evalExpr()` 只会对「纯算术表达式」走 `eval()`；一旦表达式里出现比较/逻辑运算符（`<` `<=` `>` `>=` `==` `!=` `&&` `||`），两个白名单正则都匹配不上，函数直接 `return 原字符串`。

而字符串在 JS 里是永远「真值（truthy）」，于是：

- `for (int i=1; i<=5; i++)` → 判断 `i<=5` 恒为真 → **循环跑满 `maxIter=100000` 次**才停
- `while (...)` 同样死循环到 10 万次
- `if (x>5)` 永远走 true 分支，`else` 永远走不到

**实测证据**（node 实跑 `simulateCpp`）：

```
输入: for(int i=1;i<=5;i++){ cout << i << " "; } cout<<"END";
输出: "1 2 3 4 5 ... 99999 100000 END"   ← 本应只到 5
```

也就是说，凡是带 `<`、`==`、`&&` 的例题（CSP-J 绝大部分题），在 playground 里点「运行」都会：
- 卡死浏览器/WebView 十几秒（循环 10 万次拼巨大字符串）
- 输出完全错误

**影响范围**：第 4 个 Tab「C++ 编程备考」整块不可用。这是整个产品的核心卖点之一，建议最高优先级修复。

**修复方向**：把 `evalExpr` 升级成真正的表达式求值器——先处理比较/逻辑优先级，再算算术。或最简单稳妥：把表达式安全改写后用 `Function` 计算（注意变量替换 + 布尔结果）。我可以直接给你修。

---

## 三、功能性问题（建议修）

### 🟠 P1 — 逾期任务被「静默滚动」，逾期区永远不显示

`renderToday()` 先无条件把 `date < today` 的未完成任务改成 `today`（第 1054–1050 行），**之后**才计算 `overdueItems = tasks.filter(t => t.status==='pending' && t.date < today)`（第 1062 行）。

因为早已被改成今天，这里永远为空 → 页面上「逾期未完成」那一栏（第 1076–1087 行）**永远不会渲染**。逾期任务只是悄悄混进「今日待办」，用户看不到「逾期」警示。

另外第 1061 行 `const overdue = ...` 计算后从未使用（死代码）。

**建议**：滚动前先筛出 `date < today` 作为逾期列表渲染，再决定是否滚动；或保留原日期并单独标记 `overdue` 状态。

### 🟠 P2 — `getStatsSnapshot()` 有死代码

第 801–804 行 `todayCorrectReal` 计算了一大段却从未使用（`return true` 恒真），可删。

---

## 四、体验 / 设计建议

| 项 | 现状 | 建议 |
|---|---|---|
| 同步状态 | 底栏永远显示「已同步」（绿点） | 实际只有 localStorage，**没云同步**。建议改成「本地已保存」或接真实同步，否则误导 |
| `cin` 输入 | 用 `prompt()` 弹窗读输入 | 在 WebView 里很卡且不好用；CSP-J 题大多要输入。建议加一个「程序输入」文本框，运行前注入 |
| 数据迁移 | 无 schema 版本号 | 以后改数据结构，老用户 localStorage 可能错位。建议加 `version` 字段做迁移/重置 |
| 积分奖励 | 每日挑战发奖时直接改 `total`，未触发成就重算 | 极少数情况挑战送的分够升级时，升级动画会延迟到下次动作才弹（非致命）|

**安全提示**：`evalExpr` 用 `eval()` 算算术。目前变量替换 + 字符白名单基本挡住了注入，但比较运算符修复时建议用「安全表达式求值」而非裸 `eval`，更稳。

---

## 五、已验证「没问题」的部分

- JS 语法检查通过（`new Function` 无报错）
- 数据层 `initDefaultData`、积分 `getLevelInfo`(0/250/5500 → Lv1/2/10)、统计快照、任务完成加积分 —— 在 stub 环境全部跑通无异常
- 题库/错题/统计/成长的渲染函数能正常初始化（含内置示例数据）
- XSS：`escHtml()` 在题目、答案、输出等用户内容处基本都有转义

---

## 六、下一步

最该做的一件事：**修 P0 的 C++ 运行器**（否则整个编程模块是摆设）。

要我直接动手的话，建议按这个顺序：
1. 重写 `evalExpr` 支持比较/逻辑运算符 + 正确的布尔结果（修 P0，约 30 行）
2. 修逾期任务滚动逻辑（P1）
3. 清理死代码 + 同步状态文案（P2/体验）
4. 把 `cin` 改成文本框输入

你说一声「修」，我就开始；或者你也可以只挑其中某几项。
