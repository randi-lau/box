# 学习工作台 — 修复报告

针对 `study_workspace.html` 的代码评审，本次共修复 **5 处问题**（1 个致命 + 4 个真实缺陷），全部通过 19 项自动化用例验证、整文件语法校验。

## 🔴 致命（核心功能失效）

### 1. C++ 运行器 `simulateCpp` 不支持比较/逻辑运算（导致循环与判断全部失效）
**根因**：`evalExpr` 末尾的表达式求值器只认算术运算，遇到 `<` `>` `==` `&&` `||` 等符号时，因正则白名单不含这些字符，直接把原式当字符串原样返回。而 JS 中非空字符串恒为真值，于是：
- `for (int i=1; i<=5; i++)` 的条件 `i<=5` 永远为真 → 循环跑满 10 万次才停
- `while` / `if-else` 同理全部失效

**修复**：重写 `evalExpr` 尾部，新增 `evalCmp` 递归求值器，依次处理 `||` → `&&` → `!` 否定 → `<= >= == != < >` 比较 → 算术；并把常用 C++ 内建函数（`min/max/abs/sqrt/pow/...`）注入算术 `eval` 作用域，使它们在比较式中也可用。

**验证**：`for(i=1;i<=5;...)` 现在输出 `1 2 3 4 5`（不再出现 100000）；`while(n>0){n--}`、`if/else`、`a<b && b==3`、`max(a,b)==3`、`i%2==0` 均正确。

## 🟠 真实缺陷（测试中新发现，已一并修复）

### 2. `n--` 自减正则写错 → `while` 循环无法退出
**根因**：`processAssignment` 中 `^(\w--)--;$` 误把 `(\w--)` 当作"单词字符 + 两个减号"，`n--;` 永远匹配不上，变量永不递减。
**修复**：改为 `^(\w+)--;$`。

### 3. 多变量带初始化声明未处理 → `int a=2, b=3;` 等写法变量未定义
**根因**：`processDeclaration` 只处理单变量 `name=val` 和无初始化的多变量；`int a=2, b=3;` 既不匹配单变量（值含逗号），又被 `!line.includes('=')` 的多变量分支排除，结果 `a`、`b` 从未声明。
**修复**：新增"带初始化多变量声明"分支（并排除数组初始化 `{...}` 以免误伤），支持 `int a=2, b=3;` 及混合 `int a, b=3;`。

### 4. 按表达式下标的数组访问失败 → `s = s + a[i]` 报错/乱码
**根因**：求值器只在"整条表达式就是数组访问"时才解析下标，且变量替换会把数组名替换成标量 `0`，导致 `a[i]` 在算术式中既不被解析、又被错替换。
**修复**：在 `substVars` 中**先于变量替换**做数组下标解析，用 `evalArith` 递归求出下标再替换为元素值；并在算术求值白名单中加入 `,` 与下标支持（多参函数 / `a[i]` 现在可用）。

**验证**：`int a[3]={10,20,30}; for... s=s+a[i];` → 输出 `60`；`cout<<a[1];` → `20`。

## 🟡 次要（逻辑/整洁）

### 5. 逾期任务被静默吞掉 + 两处死代码
- **逾期逻辑**：`renderToday()` 先把过期任务 `date` 改成"今天"再判断是否逾期，导致"逾期未完成"栏永远空。改为**先记录逾期任务 id**（滚动前），再滚动日期，并按 id 标记逾期项；同时把逾期项从"今日待办"中排除，避免重复。
- **死代码**：删除 `getStatsSnapshot` 中未使用的 `todayCorrectReal`，以及 `renderToday` 中未使用的 `const overdue`。

## 修复范围说明
本次聚焦"让 C++ 备考运行器与任务逾期逻辑真正可用"，**未改动**数据层、积分/等级、统计快照（这些原本正常）、UI 样式，以及 `cin` 用 `prompt()` 弹窗读输入的移动端体验问题（建议后续改为页面内输入框）。

## 验证方式
用 Node 桩环境（stub DOM / localStorage）实跑 `simulateCpp` 与 `getStatsSnapshot`，覆盖：for/while 循环、if-else、比较/逻辑/取模运算、多参内建函数、字符串比较、数组初始化与下标遍历、嵌套循环、缺 main 报错、负数、字符串拼接、无初始化数组等共 **19 项**，全部通过；整文件 `new Function` 语法校验通过。
